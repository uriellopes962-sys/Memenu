# Copy to .env.local for local development. In Vercel, set these under
# Project Settings → Environment Variables (POSTGRES_URL is auto-added
# when you connect a Postgres database from the Storage tab).

# Auto-set by the Vercel Postgres/Neon integration:
POSTGRES_URL=

# Any long random string — used to sign session cookies:
SESSION_SECRET=
